.env
.env.bak

# Escape-hatch runtime audit log (written per render; not a tracked artifact)
manim_renderer/escape_hatch/usage_log.jsonl

# Terraform local state (may contain secrets from startup script interpolation)
infra/terraform.tfstate
infra/terraform.tfstate.backup
infra/.terraform/
output/
tmp/
data/
# Provisioned asset trees (lockfile: assets/manifest.json).
# Dir names mirror the schema `kind` enum: icons | font | sfx | model.
assets/icons/
assets/font/
assets/model/
assets/sfx/*
!assets/sfx/palette.json
# Manim LaTeX/SVG cache (regenerated per render)
media/

# Python bytecode (regenerated on import; never source-of-truth)
__pycache__/
*.pyc
*.pyo

# Generated episode runs (authored artifacts + rendered media; large binaries)
episodes/
# Episode media copied into the map renderer web root at render time
map_renderer/web/episode_media/
# Test-generated artifacts: demo render output + on-demand alignment cache
tests/fixtures/demo_output/
tests/fixtures/vo_sample.words.json
# Operator media drop folder: ignore dropped files, keep the folder + docs
media_input/*
!media_input/.gitkeep
!media_input/README.md

# Claude Code workspace (per-developer plans, memory, settings)
.claude/

# MCP server configuration (per-developer; may contain auth/local paths)
.mcp.json